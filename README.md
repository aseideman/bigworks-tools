# BIG WORKS Tools

Self-contained, client-side tools embedded on bigworks.app via jsDelivr.

- `invoice-generator.html` — invoice/estimate generator with per-trade presets and dependency-free PDF download. Config via query params: `?mode=invoice|estimate&preset=<trade>`.
